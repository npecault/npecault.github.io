/**
 * Types of dialogs that can be opened.
 */
export enum DialogType {
    /**
     * A dialog that can be closed by clicking outside of it.
     */
    DIALOG,
    /**
     * A modal dialog that must be closed by the user.
     */
    MODAL
}
